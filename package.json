{
  "name": "payzen-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "build": "cd frontend && bun install && bun run build && rm -rf ../dist && cp -r dist ../dist",
    "build:dev": "cd frontend && bun install && bun run build:dev && rm -rf ../dist && cp -r dist ../dist",
    "preview": "cd frontend && bun run preview",
    "lint": "cd frontend && bun run lint",
    "test": "cd frontend && bun run test"
  }
}
